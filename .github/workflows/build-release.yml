name: Release Build

on:
  # -rc tag 时预览发布, 跳过 telegram 通知、跳过 winget 提交、跳过 latest.json 文件更新
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      core_tag:
        description: 'Chitanda Core tag (e.g. v1.19.31)'
        required: false
        default: ''
      upstream_tag:
        description: 'Verge release tag'
        required: false
        default: ''
      force_rebuild:
        description: 'Force rebuild even if assets exist'
        required: false
        type: boolean
        default: false
permissions: write-all
env:
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short
  HUSKY: 0
concurrency:
  group: '${{ github.workflow }} - ${{ github.head_ref || github.ref }}'
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  check_tag_version:
    name: Check Release Tag and package.json Version Consistency
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.check_version.outputs.tag_name }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check if tag is from main branch
        if: github.event_name != 'workflow_dispatch'
        run: |
          TAG_REF="${GITHUB_REF##*/}"
          echo "Checking if tag $TAG_REF is from main branch..."

          TAG_COMMIT=$(git rev-list -n 1 $TAG_REF)
          MAIN_COMMITS=$(git rev-list origin/main)

          if echo "$MAIN_COMMITS" | grep -q "$TAG_COMMIT"; then
            echo "✅ Tag $TAG_REF is from main branch"
          else
            echo "❌ Tag $TAG_REF is not from main branch"
            echo "This release workflow only accepts tags from main branch."
            exit 1
          fi

      - name: Check tag and package.json version
        id: check_version
        run: |
          PKG_VERSION=$(jq -r .version package.json)
          echo "package.json version: $PKG_VERSION"
          EXPECTED_TAG="v$PKG_VERSION"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG_REF="$EXPECTED_TAG"
            echo "Running via workflow_dispatch, using tag $TAG_REF"
          else
            TAG_REF="${GITHUB_REF_NAME:-${GITHUB_REF##*/}}"
            echo "Current tag: $TAG_REF"

            if [[ "$TAG_REF" != "$EXPECTED_TAG" ]]; then
              echo "❌ Version mismatch:"
              echo "   Git tag       : $TAG_REF"
              echo "   package.json  : $EXPECTED_TAG"
              exit 1
            fi
            echo "✅ Tag and package.json version are consistent."
          fi
          echo "tag_name=$TAG_REF" >> $GITHUB_OUTPUT

  prepare_release:
    name: Prepare Draft Release
    runs-on: ubuntu-latest
    needs: [check_tag_version]
    outputs:
      release_id: ${{ steps.prepare_release.outputs.release_id }}
    steps:
      - name: Prepare draft or get existing release
        id: prepare_release
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}"
          echo "Checking release for tag $TAG_NAME..."
          RELEASE_INFO=$(gh api "repos/${{ github.repository }}/releases/tags/$TAG_NAME" 2>/dev/null || true)
          if [ -n "$RELEASE_INFO" ]; then
            RELEASE_ID=$(echo "$RELEASE_INFO" | jq -r '.id // empty')
            if [ -n "$RELEASE_ID" ]; then
              echo "Release already exists with ID: $RELEASE_ID"
              echo "release_id=$RELEASE_ID" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi

          echo "Release does not exist. Creating draft release..."
          DRAFT_RES=$(gh api repos/${{ github.repository }}/releases \
            -f tag_name="$TAG_NAME" \
            -f target_commitish="${{ github.sha }}" \
            -f name="Chitanda Verge $TAG_NAME" \
            -f body="Draft release, will be updated later." \
            -F draft=true \
            -F prerelease=${{ contains(needs.check_tag_version.outputs.tag_name || github.ref_name, '-rc') }})
          RELEASE_ID=$(echo "$DRAFT_RES" | jq -r '.id')
          echo "Created draft release ID: $RELEASE_ID"
          echo "release_id=$RELEASE_ID" >> $GITHUB_OUTPUT

  update_tag:
    name: Update tag
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    needs:
      [
        check_tag_version,
        prepare_release,
        release,
        release-for-linux-arm,
        release-for-fixed-webview2,
      ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Group changelog platforms
        run: bash ./scripts-workflow/group_platforms.sh

      - name: Fetch UPDATE logs
        id: fetch_update_logs
        run: bash ./scripts/extract_update_logs.sh
        shell: bash

      - name: Set Env
        run: |
          echo "BUILDTIME=$(TZ=Asia/Shanghai date)" >> $GITHUB_ENV
          TAG_REF="${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}"
          echo "TAG_NAME=$TAG_REF" >> $GITHUB_ENV
          VERSION=$(echo "$TAG_REF" | sed 's/^v//')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "DOWNLOAD_URL=https://github.com/${{ github.repository }}/releases/download/$TAG_REF" >> $GITHUB_ENV
        shell: bash

      - run: |
          if [ -z "$UPDATE_LOGS" ]; then
            echo "No update logs found, using default message"
            UPDATE_LOGS="More new features are now supported. Check for detailed changelog soon."
          else
            echo "Using found update logs"
          fi

          cat > release.txt << EOF
          $UPDATE_LOGS

          ## 下载地址

          ### Windows (不再支持Win7)
          #### 正常版本(推荐)
          - [64位(常用)](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_x64-setup.exe) | [ARM64(不常用)](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_arm64-setup.exe)

          #### 内置Webview2版(体积较大，仅在企业版系统或无法安装webview2时使用)
          - [64位](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_x64_fixed_webview2-setup.exe) | [ARM64](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_arm64_fixed_webview2-setup.exe)

          ### macOS
          - [Apple M芯片](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_aarch64.dmg) | [Intel芯片](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_x64.dmg)

          ### Linux
          #### DEB包(Debian系) 使用 apt ./路径 安装
          - [64位](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_amd64.deb) | [ARM64](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_arm64.deb) | [ARMv7](${{ env.DOWNLOAD_URL }}/Clash.Verge_${{ env.VERSION }}_armhf.deb)

          #### RPM包(Redhat系) 使用 dnf ./路径 安装
          - [64位](${{ env.DOWNLOAD_URL }}/Clash.Verge-${{ env.VERSION }}-1.x86_64.rpm) | [ARM64](${{ env.DOWNLOAD_URL }}/Clash.Verge-${{ env.VERSION }}-1.aarch64.rpm) | [ARMv7](${{ env.DOWNLOAD_URL }}/Clash.Verge-${{ env.VERSION }}-1.armhfp.rpm)

          ### FAQ
          - [常见问题](https://clash-verge-rev.github.io/faq/windows.html)

          
          Created at ${{ env.BUILDTIME }}.
          EOF

      - name: Publish Release
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
          PRERELEASE: ${{ contains(needs.check_tag_version.outputs.tag_name || github.ref_name, '-rc') }}
        run: |
          release_id="${{ needs.prepare_release.outputs.release_id }}"

          if [ -z "$release_id" ]; then
            echo "::error::No prepared release ID found for $TAG_NAME."
            exit 1
          fi

          release_info=$(gh api repos/{owner}/{repo}/releases/"$release_id" --jq '[.tag_name, (.draft|tostring), (.assets | length), .html_url] | @tsv')
          IFS=$'\t' read -r release_tag is_draft asset_count release_url <<< "$release_info"

          if [ "$release_tag" != "$TAG_NAME" ]; then
            echo "::error::Prepared release $release_id belongs to $release_tag, expected $TAG_NAME."
            exit 1
          fi

          if [ "$is_draft" = "false" ]; then
            echo "Release $release_id ($release_url) is already published with $asset_count assets. Updating description."
            gh api --method PATCH repos/{owner}/{repo}/releases/"$release_id" \
              -f name="Chitanda Verge $TAG_NAME" \
              -F body=@release.txt \
              --silent
          else
            echo "Publishing draft release $release_id ($release_url) with $asset_count assets."
            gh api --method PATCH repos/{owner}/{repo}/releases/"$release_id" \
              -f name="Chitanda Verge $TAG_NAME" \
              -F body=@release.txt \
              -F draft=false \
              -F prerelease="$PRERELEASE" \
              --silent
          fi

  release:
    name: Release Build
    needs: [check_tag_version, prepare_release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: windows-latest
            target: aarch64-pc-windows-msvc
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu

    runs-on: ${{ matrix.os }}
    steps:
      - name: Check if asset already exists
        id: check_asset
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}"
          VERSION="${TAG#v}"
          TARGET="${{ matrix.target }}"
          case "$TARGET" in
            x86_64-pc-windows-msvc) CHECK_ASSET="Clash.Verge_${VERSION}_x64-setup.exe" ;;
            aarch64-pc-windows-msvc) CHECK_ASSET="Clash.Verge_${VERSION}_arm64-setup.exe" ;;
            aarch64-apple-darwin) CHECK_ASSET="Clash.Verge_${VERSION}_aarch64.dmg" ;;
            x86_64-apple-darwin) CHECK_ASSET="Clash.Verge_${VERSION}_x64.dmg" ;;
            x86_64-unknown-linux-gnu) CHECK_ASSET="Clash.Verge_${VERSION}_amd64.deb" ;;
            *) CHECK_ASSET="" ;;
          esac

          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ] || [ -n "${{ github.event.inputs.core_tag }}" ]; then
            echo "Force rebuild or core_tag provided. Proceeding with build."
            echo "skip_build=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ -n "$CHECK_ASSET" ]; then
            echo "Checking if asset $CHECK_ASSET exists in release $TAG..."
            ASSETS=$(gh api repos/${{ github.repository }}/releases/tags/"$TAG" --jq '.assets[].name' 2>/dev/null || true)
            if echo "$ASSETS" | grep -qx "$CHECK_ASSET"; then
              echo "✅ Asset $CHECK_ASSET already exists in release $TAG. Skipping build."
              echo "skip_build=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip_build=false" >> $GITHUB_OUTPUT

      - name: Checkout Repository
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/checkout@v7

      - name: Install Rust Stable
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: '1.98.1'
          targets: ${{ matrix.target }}

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: rustup target add ${{ matrix.target }}

      - name: Rust Cache
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/dev' }}
          prefix-key: 'v1-rust'
          key: 'rust-shared-stable-${{ matrix.os }}-${{ matrix.target }}'
          workspaces: |
            . -> target
          cache-all-crates: true
          cache-workspace-crates: true

      - name: Install dependencies (ubuntu only)
        if: matrix.os == 'ubuntu-22.04' && steps.check_asset.outputs.skip_build != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxslt1.1 libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Install Node
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/setup-node@v7
        with:
          node-version: '24.21.0'

      - uses: pnpm/action-setup@v6
        name: Install pnpm
        if: steps.check_asset.outputs.skip_build != 'true'
        with:
          run_install: false

      - name: Pnpm install and check
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          pnpm i
          pnpm run prebuild ${{ matrix.target }}

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          rustup target add ${{ matrix.target }} --toolchain 1.98.1 || rustup target add ${{ matrix.target }}
          rustup target list --installed
          echo "Rust target ${{ matrix.target }} installed."

      - name: Tauri build
        if: steps.check_asset.outputs.skip_build != 'true'
        # 上游 5.24 修改了 latest.json 的生成逻辑，且依赖 tauri-plugin-update 2.10.0 暂未发布，故锁定在 0.5.23 版本
        uses: tauri-apps/tauri-action@v1.0.0
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          GITHUB_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          releaseId: ${{ needs.prepare_release.outputs.release_id }}
          tagName: ${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}
          releaseBody: 'Draft release, will be updated later.'
          tauriScript: pnpm
          args: --target ${{ matrix.target }}
          includeUpdaterJson: true

      - name: Attest Windows bundles
        if: matrix.os == 'windows-latest' && steps.check_asset.outputs.skip_build != 'true'
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: target/${{ matrix.target }}/release/bundle/nsis/*setup*

      - name: Attest macOS bundles
        if: matrix.os == 'macos-latest' && steps.check_asset.outputs.skip_build != 'true'
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: target/${{ matrix.target }}/release/bundle/dmg/*.dmg

      - name: Attest Linux bundles
        if: matrix.os == 'ubuntu-22.04' && steps.check_asset.outputs.skip_build != 'true'
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            target/${{ matrix.target }}/release/bundle/deb/*.deb
            target/${{ matrix.target }}/release/bundle/rpm/*.rpm

  release-for-linux-arm:
    name: Release Build for Linux ARM
    needs: [check_tag_version, prepare_release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
            arch: arm64
          - os: ubuntu-22.04
            target: armv7-unknown-linux-gnueabihf
            arch: armhf
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check if asset already exists
        id: check_asset
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}"
          VERSION="${TAG#v}"
          CHECK_ASSET="Clash.Verge_${VERSION}_${{ matrix.arch }}.deb"
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ] || [ -n "${{ github.event.inputs.core_tag }}" ]; then
            echo "Force rebuild or core_tag provided. Proceeding with build."
            echo "skip_build=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Checking if asset $CHECK_ASSET exists in release $TAG..."
          ASSETS=$(gh api repos/${{ github.repository }}/releases/tags/"$TAG" --jq '.assets[].name' 2>/dev/null || true)
          if echo "$ASSETS" | grep -qx "$CHECK_ASSET"; then
            echo "✅ Asset $CHECK_ASSET already exists in release $TAG. Skipping build."
            echo "skip_build=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "skip_build=false" >> $GITHUB_OUTPUT

      - name: Checkout Repository
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/checkout@v7

      - name: Install Rust Stable
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: '1.98.1'
          targets: ${{ matrix.target }}

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: rustup target add ${{ matrix.target }}

      - name: Rust Cache
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/dev' }}
          prefix-key: 'v1-rust'
          key: 'rust-shared-stable-${{ matrix.os }}-${{ matrix.target }}'
          workspaces: |
            . -> target
          cache-all-crates: true
          cache-workspace-crates: true

      - name: Install Node
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/setup-node@v7
        with:
          node-version: '24.21.0'

      - name: Install pnpm
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - name: Pnpm install and check
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          pnpm i
          pnpm run prebuild ${{ matrix.target }}

      - name: 'Setup for linux'
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |-
          sudo ls -lR /etc/apt/

          cat > /tmp/sources.list << EOF
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-security main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-updates main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-backports main multiverse universe restricted

          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-security main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-updates main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-backports main multiverse universe restricted
          EOF

          sudo mv /etc/apt/sources.list /etc/apt/sources.list.default
          sudo mv /tmp/sources.list /etc/apt/sources.list

          sudo dpkg --add-architecture ${{ matrix.arch }}
          sudo apt update

          sudo apt install -y \
            libxslt1.1:${{ matrix.arch }} \
            libwebkit2gtk-4.1-dev:${{ matrix.arch }} \
            libayatana-appindicator3-dev:${{ matrix.arch }} \
            libssl-dev:${{ matrix.arch }} \
            patchelf:${{ matrix.arch }} \
            librsvg2-dev:${{ matrix.arch }}

      - name: 'Install aarch64 tools'
        if: matrix.target == 'aarch64-unknown-linux-gnu' && steps.check_asset.outputs.skip_build != 'true'
        run: |
          sudo apt install -y \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu

      - name: 'Install armv7 tools'
        if: matrix.target == 'armv7-unknown-linux-gnueabihf' && steps.check_asset.outputs.skip_build != 'true'
        run: |
          sudo apt install -y \
            gcc-arm-linux-gnueabihf \
            g++-arm-linux-gnueabihf

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          rustup target add ${{ matrix.target }} --toolchain 1.98.1 || rustup target add ${{ matrix.target }}
          rustup target list --installed
          echo "Rust target ${{ matrix.target }} installed."

      - name: Build for Linux
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          export PKG_CONFIG_ALLOW_CROSS=1
          if [ "${{ matrix.target }}" == "aarch64-unknown-linux-gnu" ]; then
            export PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig/:$PKG_CONFIG_PATH
            export PKG_CONFIG_SYSROOT_DIR=/usr/aarch64-linux-gnu/
          elif [ "${{ matrix.target }}" == "armv7-unknown-linux-gnueabihf" ]; then
            export PKG_CONFIG_PATH=/usr/lib/arm-linux-gnueabihf/pkgconfig/:$PKG_CONFIG_PATH
            export PKG_CONFIG_SYSROOT_DIR=/usr/arm-linux-gnueabihf/
          fi
          pnpm build --target ${{ matrix.target }}
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: Attest Linux bundles
        if: steps.check_asset.outputs.skip_build != 'true'
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            target/${{ matrix.target }}/release/bundle/deb/*.deb
            target/${{ matrix.target }}/release/bundle/rpm/*.rpm

      - name: Upload release assets
        if: steps.check_asset.outputs.skip_build != 'true'
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}" \
            target/${{ matrix.target }}/release/bundle/deb/*.deb \
            target/${{ matrix.target }}/release/bundle/rpm/*.rpm \
            --clobber

  release-for-fixed-webview2:
    name: Release Build for Fixed WebView2
    needs: [check_tag_version, prepare_release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            arch: x64
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            arch: arm64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check if asset already exists
        id: check_asset
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}"
          VERSION="${TAG#v}"
          CHECK_ASSET="Clash.Verge_${VERSION}_${{ matrix.arch }}_fixed_webview2-setup.exe"
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ] || [ -n "${{ github.event.inputs.core_tag }}" ]; then
            echo "Force rebuild or core_tag provided. Proceeding with build."
            echo "skip_build=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Checking if asset $CHECK_ASSET exists in release $TAG..."
          ASSETS=$(gh api repos/${{ github.repository }}/releases/tags/"$TAG" --jq '.assets[].name' 2>/dev/null || true)
          if echo "$ASSETS" | grep -qx "$CHECK_ASSET"; then
            echo "✅ Asset $CHECK_ASSET already exists in release $TAG. Skipping build."
            echo "skip_build=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "skip_build=false" >> $GITHUB_OUTPUT

      - name: Checkout Repository
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/checkout@v7

      - name: Install Rust Stable
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: '1.98.1'
          targets: ${{ matrix.target }}

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: rustup target add ${{ matrix.target }}

      - name: Rust Cache
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/dev' }}
          prefix-key: 'v1-rust'
          key: 'rust-shared-stable-${{ matrix.os }}-${{ matrix.target }}'
          workspaces: |
            . -> target
          cache-all-crates: true
          cache-workspace-crates: true

      - name: Install Node
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: actions/setup-node@v7
        with:
          node-version: '24.21.0'

      - uses: pnpm/action-setup@v6
        name: Install pnpm
        if: steps.check_asset.outputs.skip_build != 'true'
        with:
          run_install: false

      - name: Pnpm install and check
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          pnpm i
          pnpm run prebuild ${{ matrix.target }}

      - name: Download WebView2 Runtime
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          invoke-webrequest -uri https://github.com/westinyang/WebView2RuntimeArchive/releases/download/133.0.3065.92/Microsoft.WebView2.FixedVersionRuntime.133.0.3065.92.${{ matrix.arch }}.cab -outfile Microsoft.WebView2.FixedVersionRuntime.133.0.3065.92.${{ matrix.arch }}.cab
          Expand .\Microsoft.WebView2.FixedVersionRuntime.133.0.3065.92.${{ matrix.arch }}.cab -F:* ./src-tauri
          Remove-Item .\src-tauri\tauri.windows.conf.json
          Rename-Item .\src-tauri\webview2.${{ matrix.arch }}.json tauri.windows.conf.json

      - name: Add Rust Target
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          rustup target add ${{ matrix.target }} --toolchain 1.98.1 || rustup target add ${{ matrix.target }}
          rustup target list --installed
          echo "Rust target ${{ matrix.target }} installed."

      - name: Tauri build
        if: steps.check_asset.outputs.skip_build != 'true'
        uses: tauri-apps/tauri-action@v1.0.0
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          GITHUB_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tauriScript: pnpm
          args: --target ${{ matrix.target }}

      - name: Rename
        if: steps.check_asset.outputs.skip_build != 'true'
        run: |
          $files = Get-ChildItem ".\target\${{ matrix.target }}\release\bundle\nsis\*-setup.exe"
          foreach ($file in $files) {
            $newName = $file.Name -replace "-setup\.exe$", "_fixed_webview2-setup.exe"
            Rename-Item $file.FullName $newName
          }

          $files = Get-ChildItem ".\target\${{ matrix.target }}\release\bundle\nsis\*.nsis.zip"
          foreach ($file in $files) {
            $newName = $file.Name -replace "-setup\.nsis\.zip$", "_fixed_webview2-setup.nsis.zip"
            Rename-Item $file.FullName $newName
          }

          $files = Get-ChildItem ".\target\${{ matrix.target }}\release\bundle\nsis\*-setup.exe.sig"
          foreach ($file in $files) {
            $newName = $file.Name -replace "-setup\.exe\.sig$", "_fixed_webview2-setup.exe.sig"
            Rename-Item $file.FullName $newName
          }

      - name: Attest Windows bundles
        if: steps.check_asset.outputs.skip_build != 'true'
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: target/${{ matrix.target }}/release/bundle/nsis/*setup*

      - name: Upload release assets
        if: steps.check_asset.outputs.skip_build != 'true'
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          $files = Get-ChildItem "target\${{ matrix.target }}\release\bundle\nsis\*setup*" | Select-Object -ExpandProperty FullName
          gh release upload "${{ needs.check_tag_version.outputs.tag_name || github.ref_name }}" $files --clobber
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  release-update:
    if: ${{ !cancelled() && !contains(needs.check_tag_version.outputs.tag_name || github.ref_name, '-rc') }}
    name: Release Update
    runs-on: ubuntu-latest
    needs: [check_tag_version, update_tag]
    continue-on-error: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Node
        uses: actions/setup-node@v7
        with:
          node-version: '24.21.0'

      - uses: pnpm/action-setup@v6
        name: Install pnpm
        with:
          run_install: false

      - name: Pnpm install
        run: pnpm i

      - name: Release updater file
        run: pnpm updater
        env:
          GITHUB_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}

  release-update-for-fixed-webview2:
    if: ${{ !cancelled() && !contains(needs.check_tag_version.outputs.tag_name || github.ref_name, '-rc') }}
    name: Release Update for Fixed WebView2
    runs-on: ubuntu-latest
    needs: [check_tag_version, update_tag]
    continue-on-error: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Node
        uses: actions/setup-node@v7
        with:
          node-version: '24.21.0'

      - uses: pnpm/action-setup@v6
        name: Install pnpm
        with:
          run_install: false

      - name: Pnpm install
        run: pnpm i

      - name: Release updater file
        run: pnpm updater-fixed-webview2
        env:
          GITHUB_TOKEN: ${{ secrets.DISPATCH_TOKEN || secrets.GITHUB_TOKEN }}
